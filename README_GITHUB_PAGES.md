# Prompt Builder 0.1.62 - GitHub Pages

The app is fully static. `Category` and `AI Tool` values are derived from `prompt_templates.json`. Mutable user data is stored in browser localStorage.


User presets are included in Export / Import user data and stored locally in this browser.
